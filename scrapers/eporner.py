import requests
from bs4 import BeautifulSoup

def scrape_eporner(query):
    results = []
    # Simulated fixed logic
    results.append({
        "title": f"eporner Result for '{{query}}'",
        "url": "https://site.com",
        "preview": "https://via.placeholder.com/300x160",
        "source": "eporner"
    })
    return results
